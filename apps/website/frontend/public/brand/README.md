# Lina identity assets

These are synchronized consumer copies. The canonical SVG and exports live in packages/brand at the repository root. Run npm run brand:sync from the root, or npm run brand:export after editing the source SVG.
