# Hands-free voice and scoped Orchestrator controls

- Optional Hey Vibe activation uses bundled CPU models for wake detection, speech activity, and turn completion. Space push-to-talk remains available, with ordered audio flushing and capture ownership checks.
- Orchestrator interprets natural requests into scoped actions, preserves unfinished work across clarifications, and supports native terminal controls with generation, screen, and supplied-answer checks. Private diagnostics help trace failures without recording keys or microphone content.
- The dashboard has clearer session activity, stable recency, and bounded bubble motion.
- Review fixes release microphones after failed startup, time out stalled inference, accept fresh terminal observations, and restore the default Documents folder for project creation.
- Terminal links use the external browser. The app rejects replacement pages and popups while preserving normal reloads. Failed version-installer launches leave the app open with an error.
- Electron and vulnerable runtime/build dependencies are updated. Release checks now cover native model fixtures, keyword accuracy cases, native dependencies, and inference inside the packaged app.

Automated verification covers backend contracts, renderer behavior, real local PTYs, synthetic microphone capture, native inference, and Windows package/feed integrity. Physical microphone accuracy, older-PC performance, and live model-provider interpretation remain unverified. The Windows installer remains unsigned.

Use Check for update, then Update and Restart to install.
