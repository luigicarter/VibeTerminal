Lina Terminal 0.1.117 speeds up the Orchestrator, steadies Windows terminal panes, and repairs the packaged Open Codex launcher.

- Continue the agent working on a named task in one step, without extra searching.
- Read honest confirmations: input is reported as delivered with the agent result still pending, instead of finished.
- Hear why a request could not be interpreted, with the greeting removed on every voice path and more spellings of the name recognized.
- Keep Codex terminal panes steady on Windows, with no stray console process left behind when a pane exits.
- Open Codex Web Work models in genuine Work sessions, keep regular Chat models in Temporary Chat, and block answers from an unconfirmed model.
- Start Open Codex from the installed app.
- Use the redesigned voice indicator and stop the current voice turn with Enter or a click.
- Check for update keeps the normal download and restart flow; the in-app version list was removed.

Existing settings and workspaces remain in the same application profile. Applying the update uses the normal Update/Restart controls.
