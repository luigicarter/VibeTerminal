# Lina Terminal 0.1.107

- Prevent saved task history from overflowing the Orchestrator's context budget and blocking new requests. Preserve the recent exchange needed to understand follow-ups.
- Keep coding-terminal feature requests intact when you clarify the desired behavior or prompt wording.
- Verify delegated prompt delivery even when the model omits its follow-up terminal read. Keep agent results and necessary clarifications tracked separately.
- Allow more time for native startup and recover from explicitly unsent screen-change rejections without duplicating prompts or overriding changed input.
- Explain context-budget failures with a specific offline voice alert instead of the generic request-failed message.
- Play a short listening cue when recording starts, distinct from the existing completion bell.

Choose **Check for update**, then **Update** and **Restart** when your workspace is ready.
