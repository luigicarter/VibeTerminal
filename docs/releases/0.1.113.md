Claude terminal status and terminal history are more reliable.

- Finished Claude child tasks settle when native task results or later background snapshots confirm they ended. Active background agents and commands remain visible after the main response.
- Terminal panes retain up to 5,000 scrollback rows through repeated redraws and reattachment. Restored output preserves styles, Unicode, alternate screens, cursor and input modes, with later output replayed in order.

Claude status improvements apply to newly launched panes after updating. Root responses remain provisional, and the separate child approval matching limitation documented in the investigation remains.

Install through **Check for update → Update → Restart**. Publication does not restart active workspaces.
